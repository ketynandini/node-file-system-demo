Node.js File System Demo
A simple Node.js script that uses the built-in `fs` module to create and write a text file.

How to Run
node demo.js

What It Does
- Imports the built-in `fs` module
- Writes text content to `message.txt`
- Handles errors gracefully

Requirements
Node.js installed — check with `node -v`
